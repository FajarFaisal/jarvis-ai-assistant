# ============================================================
# JARVIS - Full Upgrade Requirements
# Install with: pip install -r requirements.txt
# ============================================================

# --- Core AI ---
anthropic>=0.25.0          # Claude AI brain

# --- Speech Recognition ---
SpeechRecognition>=3.10.0  # Microphone input
openai-whisper>=20231117   # Local Whisper STT (offline capable)
pyaudio>=0.2.14            # Microphone access

# --- Text-to-Speech ---
pyttsx3>=2.90              # Offline TTS (fallback)
elevenlabs>=1.2.0          # Premium voice (ElevenLabs API)

# --- Wake Word Detection ---
pvporcupine>=3.0.0         # "Hey JARVIS" wake word (Picovoice)

# --- Smart Home ---
requests>=2.31.0           # HTTP for Home Assistant API

# --- GUI / HUD ---
PyQt5>=5.15.9              # JARVIS HUD overlay

# --- Memory / Database ---
chromadb>=0.4.22           # Vector memory (long-term recall)
SQLAlchemy>=2.0.0          # Structured memory / logs

# --- Web Search ---
tavily-python>=0.3.0       # Tavily web search API

# --- Computer Control (NEW) ---
pyautogui>=0.9.54          # Mouse, keyboard, screenshot control
selenium>=4.18.0           # Browser automation
webdriver-manager>=4.0.0   # Auto-installs ChromeDriver
pyperclip>=1.8.2           # Clipboard read/write
keyboard>=0.13.5           # Global keyboard hooks
pycaw>=20240210            # Windows volume control

# --- Gmail API (NEW) ---
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# --- Utilities ---
python-dotenv>=1.0.0       # Load .env API keys
psutil>=5.9.0              # System info (CPU, RAM, etc.)
pygame>=2.5.0              # Audio playback (startup sounds)
numpy>=1.24.0              # Audio processing
playsound>=1.3.0           # Simple sound playback
